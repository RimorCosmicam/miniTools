<?xml version="1.0" encoding="utf-8"?>
<!-- The same black rectangle, naming the tool it opens rather than the app it lives in. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/launcher_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:gravity="center_vertical|start"
    android:orientation="vertical"
    android:paddingStart="22dp"
    android:paddingEnd="14dp"
    android:paddingTop="44dp"
    android:paddingBottom="28dp">

    <ImageView
        android:layout_width="88dp"
        android:layout_height="88dp"
        android:layout_marginBottom="16dp"
        android:contentDescription="miniTools Launcher"
        android:src="@drawable/minitools_brand" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/poppins_black"
        android:text="LAUNCHER"
        android:textColor="#FFFFFF"
        android:textSize="20sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:fontFamily="@font/poppins_black"
        android:letterSpacing="0.045"
        android:text="EVERY APP, ON THE COVER SCREEN"
        android:textColor="#9EFFFFFF"
        android:textSize="11sp" />
</LinearLayout>
